#!/bin/bash

#############################################################################
# find dos text files
#############################################################################

for arg in $*; do
  kountCr=$(cat $arg | tr -d '\n' | tr '\r' '\n' | wc -l )
  (( $kountCr == 0 )) || echo $arg
done
